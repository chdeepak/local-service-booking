# Local environment variables
# Copy to .env (not committed) and update values for local development

# Preferred: full DATABASE_URL
DATABASE_URL=postgres://user:password@localhost:5432/booking

# Alternatively use individual variables
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=booking
DB_PORT=5432
DB_SSL=false

NODE_ENV=development
PORT=3000
